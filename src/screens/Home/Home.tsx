import React, {useMemo, useEffect} from 'react';
import {
  Text,
  View,
  ImageBackground,
  SafeAreaView,
  Image,
  StatusBar,
} from 'react-native';

const Home = () => {
  return <View />;
};

export default Home;
